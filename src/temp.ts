// if (boxType === 'obstacle') {
//         // editor.toggleObstacle(cellX, cellY, 'wall');
//         return;
//       }
//       if (boxType === 'start') {
//         // if (isStartFilled === null || isStartFilled === editor.findBox(cellX, cellY))
//         //   isStartFilled = editor.toggleGround(cellX, cellY, 'user');
//         return;
//       }
//       if (boxType === 'end') {
//         // if (isEndFilled === null || isEndFilled === editor.findBox(cellX, cellY))
//         //   isEndFilled = editor.toggleGround(cellX, cellY, 'place');
//         return;
//       }